tunnel: qiyadon-api
log-directory: /tmp/cloudflared-logs
metrics: 0.0.0.0:20250

ingress:
  - hostname: api.qiyadon.com
    service: http://localhost:3001
    originRequest:
      noTLSVerify: true
  - service: http_status:404
