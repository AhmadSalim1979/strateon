# 2026-05-27 — Daily Memory

## Session Overview
New session starting 2026-05-27 02:08 UTC (04:08 Berlin). No prior SESSION-STATES found for today. System bootstrapping fresh.

## Worker Heartbeat Status
- PID: 3095952 | Uptime: 6h | Status: **ONLINE but HEARTBEAT STALE**
- Last heartbeat written: 2026-05-26T06:22 UTC (~20h ago)
- Threshold: 60s → **EXCEEDED**
- Operational-state.json: last updated 2026-05-26T06:21 UTC (stale)

## WhatsApp 499 Flapping — 2026-05-27 10:07 UTC

**Pattern:** Gateway receiving status 499 every ~60 seconds since ~11:37 Berlin
**Duration:** ~30 minutes continuous flap (11:37–12:07)
**Gateway recovery:** Auto-restarts after each 499, reconnects within ~4 seconds
**Cause:** WhatsApp webhook provider returning 499 Service Unavailable

```
Gateway log shows:
"No messages received in 51-60m - restarting connection"
"Web connection closed (status 499). Retry 1/12 in 2.xs…"
```

**Status:** Gateway supervisor active, recovery automatic, no manual intervention required.
This is the SAME flapping issue from May 26 (noted in MEMORY.md persistent blockers).
Not escalating — gateway recovers continuously, no data loss.

## PM2 Services Status
All online: moosa-worker (PID 3095952), moosa-watchdog, openclaw-gateway, qiyadon-audit-form. cloudflared-tunnel stopped (disabled).

## WhatsApp
Gateway connected as +923215139934 at 07:58 Berlin. Fresh connection.

## GPU Lifecycle Alert — 2026-05-27 06:08 UTC

**Pod ID:** `23a9nue4xq4r4p` (A40, "progressive_rose_monkey-migration-migration")
**Current state:** `desiredStatus: EXITED` — pod is stopped
**Problem:** Start operation failing — `/v1/pod/23a9nue4xq4r4p/start` returns 404 Not Found
**GraphQL `podStart`:** Not available in schema (only `podStop` and `podReset` exist)
**GraphQL `podStop`:** Confirmed working

**Key diagnostic results:**
```
# Pod status (GraphQL query):
desiredStatus: "EXITED" — pod stopped normally, not failed

# Start via REST — FAILS:
curl /v1/pod/23a9nue4xq4r4p/start → 404 Not Found

# Start via GraphQL mutation → "Cannot query field 'podStart'"
mutation { podStart(input: { podId: "23a9nue4xq4r4p" }) } → ERROR

# Stop via GraphQL — WORKS:
mutation { podStop(input: { podId: "23a9nue4xq4r4p" }) } → {"desiredStatus":"EXITED"}
```

**Secrets mismatch (for reference):**
- `runpod.json` secrets: `pod_id: "c1as99lq8xtphy"` (RTX 4090 pod — moosa-brain-node-01)
- Controller hardcoded: `POD_ID: '23a9nue4xq4r4p'` (A40 pod — progressive_rose_monkey)

**Root cause assessment:** Pod 23a9nue4xq4r4p cannot be started via API. The `start` REST endpoint does not exist (404). GraphQL has no `podStart` mutation. Only `podStop` and `podReset` are available. This is a RunPod API limitation — the pod type may not support remote start via API, or the account may lack start permissions on this specific pod.

**Cron `refresh` action:** The `operational-mode.js` script DOES support `refresh` (line 228). The cron was calling `refresh-scheduler.sh` which may not exist. Lower priority.

## Pending Commitments
| Priority | Action | Owner |
|---|---|---|
| 🔴 CRITICAL | RunPod pod start — investigate manually at runpod.io | Ahmad |
| 🟡 MEDIUM | Investigate `refresh-scheduler.sh` — cron references non-existent script | Moosa |

## 15:55 Berlin — Heartbeat Check

**System restart detected:**
- moosa-worker: PID 3095952 → **PID 3326613** (restart ~13:25 UTC, uptime 29m)
- openclaw-gateway: restarted alongside worker
- WhatsApp connected fresh at 15:25 Berlin

**GPU lifecycle:** Still `UNAVAILABLE` — no change since 06:08 alert sent to Ahmad.

**C-suite sessions today:** 0 (no sessions spawned)

**Pending commitments:** Ahmad still needs to investigate RunPod pod start manually at runpod.io.

## 22:46 UTC — Night Heartbeat Check

**Worker:** PID 3451924, uptime ~116m, online
**All PM2:** online (cloudflared-tunnel, moosa-watchdog, moosa-web-console, moosa-worker, openclaw-gateway, qiyadon-audit-form)
**GPU lifecycle:** DEGRADED — no resolution since morning alert
**WhatsApp:** Active (heartbeat received)

**No new action required. Pending commitments unchanged.**