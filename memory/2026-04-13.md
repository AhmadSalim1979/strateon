# Memory — 2026-04-13

## Thinking Loop Cron — 7:00 AM UTC / 9:00 AM Berlin

### Run results
- Cron task ID: `033284be-8d95-4fea-85e6-3ef9e70265ef`
- Guard blocked: `true` → no task created
- Worker claimed: yes
- Task completed: yes
- Final decision: `no_action`
- Execution status: `advisory`
- Error: none

### Interpretation
- System healthy, advisory mode working as expected
- Guard correctly blocking task creation in advisory mode (Phase 4A.1 design)
- No issues.

## Prior Days Context
- 2026-04-12: Phase 4C enabled (shadow), Phase 5D implemented (shadow), Phase 4B.2 validation complete
- All phases stable, no issues

## Phase 5D.2 — Observability Hardening

**Task:** Make Phase 5D shadow output visible and auditable through the standard persisted thinking-loop output path (`tasks.output_json`).

**Problem identified:** Phase 5D shadow output was not appearing in `tasks.output_json` for standard thinking-loop cycles.

**Root cause:** Two issues:
1. Phase 5D was only called when `phase5COutput` was truthy — for score=0 chains, all upstream phases returned null, so Phase 5D was never reached
2. Only `phase_5d_shadow` nested object was being set; flattened fields (`phase_5d_authority_class_shadow`, etc.) were not being populated in the result object before persistence

**Fixes applied in** `src/handlers/thinking-loop.js`:

1. **Phase 5D fallback path** (line ~2669): Added `phase5CFallback` that constructs minimal Phase 5C-equivalent output from `analyzeChain` result when upstream phases return null. Ensures Phase 5D always runs and classifies authority.

2. **Flattened field population** (line ~2673): After setting `result.phase_5d_shadow`, now also explicitly assigns 7 flattened fields for direct DB visibility:
   - `phase_5d_authority_class_shadow`
   - `phase_5d_action_eligibility_shadow`
   - `phase_5d_approval_required_shadow`
   - `phase_5d_blocked_reason_shadow`
   - `phase_5d_downstream_permitted_shadow`
   - `phase_5d_rule_fired_shadow`
   - `phase_5d_shadow_mode_shadow`

**Verification:** Task `f3f442b7-7761-4448-96e9-0fff4f51a8d6` confirmed worker-processed with all 8 Phase 5D fields persisted in `tasks.output_json`. Fields: `authority_class=recommend_only`, `downstream_permitted=false`, `rule_fired=R-BLOCK-3`, `shadow_mode=true`.

**Recommendation:** Remain in shadow. Phase 5D authority boundary is now visible through the standard audit path. Ready for Phase 5D.3 design.

## Phase 5E — Implementation (Bounded Proposal Envelope Layer)

**Task:** Implement Phase 5E as the bounded proposal-envelope layer, strictly downstream of Phase 5D.

**Status:** COMPLETED

**What was implemented** in `src/handlers/thinking-loop.js`:

1. `runPhase5E(phase5DOutput, phase5CRecommendation)` function (line ~1930)
   - Pure function, isolated-testable without full thinking loop
   - Reads `FEATURE_PHASE_5E_ENABLED` and `PHASE_5E_SHADOW_MODE` fresh from env each call
   - GATE_0: `FEATURE_PHASE_5E_ENABLED !== 'true'` → null noop
   - GATE_1: `!phase5DOutput || typeof !== 'object'` → null noop
   - 4 approved proposal classes only: `none`, `analysis_only`, `proposal_available`, `proposal_pending_approval`
   - Fail-closed on unknown/invalid authority_class

2. **Deterministic 10-row truth table (5 authority classes × 2 shadow modes):**
   | authority_class | shadow | proposal_class | executor_permitted |
   |---|---|---|---|
   | blocked | * | none | false |
   | observe_only | shadow | analysis_only | false |
   | observe_only | live | none | false |
   | recommend_only | shadow | analysis_only | false |
   | recommend_only | live | none | false |
   | human_approval_required | shadow | analysis_only | false |
   | human_approval_required | live | proposal_pending_approval | false |
   | bounded_task_allowed | shadow | analysis_only | false |
   | bounded_task_allowed | live | proposal_available | **true** (only valid row) |

3. **Additional fail-closed check:** `authority_class=bounded_task_allowed` with `downstream_permitted !== true` → `none` (catches inconsistent state)

4. **Shadow mode enforcement:** `PHASE_5E_SHADOW_MODE` is the governing variable; shadow mode always produces `analysis_only` regardless of Phase 5D's `shadow_mode` field

5. **Phase 5E wired into `runThinkingLoop`** after Phase 5D block (line ~2893)
   - Flattened shadow fields added: `phase_5e_proposal_class_shadow`, `phase_5e_executor_permitted_shadow`, `phase_5e_proposal_status_shadow`, `phase_5e_approval_required_shadow`, `phase_5e_rule_fired_shadow`, `phase_5e_proposal_basis_shadow`, `phase_5e_shadow_mode_shadow`

6. `buildPhase5EOutput()` helper with full 10-field schema: `proposal_class`, `executor_permitted`, `proposal_status`, `derived_authority_class`, `derived_downstream_permitted`, `derived_approval_required`, `rule_fired`, `proposal_basis`, `shadow_mode`, `audit_metadata`

**Validation results:** 43 tests passed / 43 total
- Truth table (shadow + live): all 10 rows correct
- Shadow blocks proposal_available: confirmed
- Downstream_permitted consistency: confirmed fail-closed
- Fail-closed on invalid/null/undefined input: confirmed
- Feature flag off → null: confirmed
- Schema completeness (10 fields + audit_metadata): confirmed
- Invariant: `executor_permitted=true` only in `bounded_task_allowed+live`: confirmed
- `proposal_basis` non-empty for all 5 classes: confirmed
- `proposal_status` values correct: `not_present`, `shadowed`, `available`, `awaiting_approval`: confirmed

**Contradictions/edge cases found and fixed:**
- Initial `PHASE_5E_SHADOW_MODE` constant evaluated once at module load (wrong); fixed to read `process.env` fresh per call
- Missing `downstream_permitted` consistency check for `bounded_task_allowed` (initial test failed); fixed by adding explicit validation

**Current state:** Phase 5E starts in forced shadow mode (`PHASE_5E_SHADOW_MODE` defaults to true). `proposal_available` must NOT surface live until separately approved.

**Constraints respected (zero exceptions):**
- Never executes, never creates tasks
- Never selects concrete task types
- Never broadens authority beyond Phase 5D confirmed boundary
- Fail-closed on any unknown/invalid input
- `executor_permitted=true` only in the one valid row

**Next (when approved):** Phase 5E validation suite, aggressive fail test suite, shadow output verification tests, shadow rollout recommendation
- Two workers running (PID 1070548 and 1070550, started ~10:02)
- Both using JWT: `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...0yQ` (service role)
- Supabase connection works from Node.js (sb_secret key format works internally)
- All phases operational

## Phase 5F — Executor Readiness / Action Materialization Layer

**Task:** Implement Phase 5F as the bounded action-materialization layer, strictly downstream of Phase 5E.

**Status:** COMPLETED

**What was implemented** in `src/handlers/thinking-loop.js`:

1. `runPhase5F(phase5EOutput)` function (line ~2218)
   - Pure function, isolated-testable
   - Reads `FEATURE_PHASE_5F_ENABLED` and `PHASE_5F_SHADOW_MODE` fresh from env each call
   - GATE_0: `FEATURE_PHASE_5F_ENABLED !== 'true'` → null noop
   - GATE_1: `!phase5EOutput || typeof !== 'object'` → null noop
   - 4 materialization classes: `none`, `draft_action`, `action_pending_approval`, `executor_ready`
   - `executor_ready=true` only in `proposal_available + live` row
   - `approval_required=true` when `proposal_class=proposal_pending_approval`

2. **Deterministic 8-row truth table:**
   | Phase5E proposal_class | Shadow | materialization_class | executor_ready | approval_required |
   |---|---|---|---|---|
   | none | * | none | false | false |
   | analysis_only | shadow | draft_action | false | false |
   | analysis_only | live | draft_action | false | false |
   | proposal_pending_approval | shadow | draft_action | false | true |
   | proposal_pending_approval | live | action_pending_approval | false | true |
   | proposal_available | shadow | draft_action | false | false |
   | proposal_available | live | executor_ready | **true** | false |

3. **Wired into `runThinkingLoop`** after Phase 5E
   - Flattened shadow fields: `phase_5f_materialization_class_shadow`, `phase_5f_executor_ready_shadow`, `phase_5f_approval_required_shadow`, `phase_5f_materialized_action_ref_shadow`, `phase_5f_materialization_basis_shadow`, `phase_5f_rule_fired_shadow`, `phase_5f_shadow_mode_shadow`

4. `buildPhase5FOutput()` — 11-field schema: `materialization_class`, `executor_ready`, `approval_required`, `materialized_action_ref`, `materialization_basis`, `derived_authority_class`, `derived_proposal_class`, `derived_executor_permitted`, `rule_fired`, `shadow_mode`, `audit_metadata`

**Validation results:** 8-row truth table all correct; all 28 aggressive fail tests passed
**Persisted shadow fields confirmed in Supabase `tasks.output_json`**

## Phase 5G — Execution Trigger / Controlled Dispatch Layer

**Task:** Implement Phase 5G as the bounded dispatch-permission signal layer, strictly downstream of Phase 5F.

**Status:** COMPLETED

**What was implemented** in `src/handlers/thinking-loop.js`:

1. `runPhase5G(phase5FOutput)` function (line ~2420)
   - Pure function, isolated-testable
   - 4 dispatch classes: `none`, `dispatch_draft`, `dispatch_pending_approval`, `dispatch_ready`
   - `dispatch_permitted=true` only in `executor_ready + !approval_required + !shadow` row
   - `idempotency_required=true` signals downstream to enforce idempotency

2. **Deterministic 8-row truth table:**
   | Phase5F materialization_class | executor_ready | approval_required | Shadow | dispatch_class | dispatch_permitted |
   |---|---|---|---|---|---|
   | none | false | false | * | none | false |
   | draft_action | false | false | shadow | dispatch_draft | false |
   | draft_action | false | false | live | dispatch_draft | false |
   | action_pending_approval | false | true | shadow | dispatch_draft | false |
   | action_pending_approval | false | true | live | dispatch_pending_approval | false |
   | executor_ready | true | false | shadow | dispatch_draft | false |
   | executor_ready | true | false | live | dispatch_ready | **true** |

3. **Wired into `runThinkingLoop`** after Phase 5F
   - Flattened shadow fields: `phase_5g_dispatch_class_shadow`, `phase_5g_dispatch_permitted_shadow`, `phase_5g_approval_required_shadow`, `phase_5g_idempotency_required_shadow`, `phase_5g_dispatch_ref_shadow`, `phase_5g_dispatch_basis_shadow`, `phase_5g_rule_fired_shadow`, `phase_5g_shadow_mode_shadow`

4. `buildPhase5GOutput()` — 12-field schema with audit_metadata

**Validation results:** 8-row truth table all correct; all 28 aggressive fail tests passed
**Persisted shadow fields confirmed in Supabase `tasks.output_json`**

**Key invariant:** `dispatch_permitted=true` only in the one valid row — never leaks in shadow mode.

## Phase 5H — Dispatch Creation Readiness Layer

**Task:** Implement Phase 5H as the bounded dispatch-creation-readiness decision layer, strictly advisory (no persistence), downstream of Phase 5G.

**Status:** COMPLETED

**What was implemented** in `src/handlers/thinking-loop.js`:

1. `runPhase5H(phase5GOutput, taskId)` function (line ~2580)
   - Pure function, isolated-testable, zero database dependency
   - `taskId` for `idempotency_key_candidate` composition (advisory only)
   - 4 creation classes: `none`, `creation_draft`, `creation_pending_approval`, `creation_ready`
   - `creation_permitted=true` only in `dispatch_ready + !shadow` row
   - `idempotency_key_candidate = dispatchRef + ':' + taskId` (advisory, not enforced)
   - `persistence_required=true` when `creation_class != none`

2. **Deterministic 8-row truth table:**
   | Phase5G dispatch_class | Shadow | creation_class | creation_permitted | persistence_required |
   |---|---|---|---|---|
   | none | * | none | false | false |
   | dispatch_draft | shadow | creation_draft | false | true |
   | dispatch_draft | live | creation_draft | false | true |
   | dispatch_pending_approval | shadow | creation_draft | false | true |
   | dispatch_pending_approval | live | creation_pending_approval | false | true |
   | dispatch_ready | shadow | creation_draft | false | true |
   | dispatch_ready | live | creation_ready | **true** | true |

3. **Wired into `runThinkingLoop`** after Phase 5G (line ~3661)
   - Uses `result.task?.id ?? null` for taskId (correct scope)
   - Flattened shadow fields: `phase_5h_creation_class_shadow`, `phase_5h_creation_permitted_shadow`, `phase_5h_persistence_required_shadow`, `phase_5h_idempotency_key_candidate_shadow`, `phase_5h_dispatch_record_candidate_ref_shadow`, `phase_5h_creation_basis_shadow`, `phase_5h_derived_dispatch_class_shadow`, `phase_5h_derived_dispatch_permitted_shadow`, `phase_5h_rule_fired_shadow`, `phase_5h_shadow_mode_shadow`

4. `buildPhase5HOutput()` — 13-field schema with audit_metadata
5. `_computeIdempotencyKey(dispatchRef, taskId)` — advisory key composition

**Validation results:** 28 validation tests + 30 aggressive fail tests all passed
**Persisted shadow fields (11 total) confirmed in Supabase `tasks.output_json`**

**Architecture:** Phase 5H is purely advisory — does NOT create records, does NOT write to any table. Actual record persistence belongs to Phase 5I (future).

**Key design decisions:**
- Missing `taskId` → `idempotency_key_candidate=null` (fail-safe, no usable key)
- Missing `dispatchRef` → `idempotency_key_candidate=null`
- `normalizeBool()` applied to `dispatch_permitted` and `approval_required` from Phase 5G inputs

## PM2 / Gateway Config State

- Gateway: running (PID 1077854), uptime ~3h, port 18789, bind loopback
- Workers managed via PM2 module conf: `/root/.pm2/module_conf.json`
- moosa-worker env vars (PM2 module conf):
  - `SUPABASE_URL`, `SUPABASE_SERVICE_KEY` (sb_secret format)
  - `FEATURE_PHASE_5D_ENABLED=true`, `PHASE_5D_SHADOW_MODE=true`
  - `FEATURE_PHASE_5H_ENABLED=true`, `PHASE_5H_SHADOW_MODE=true`
- ecosystem.config.js in `/root/.openclaw/workspace/moosa-worker/` (backup reference only)

## Phase Architecture Summary (as of end of session)

| Phase | Name | Status | Shadow |
|---|---|---|---|
| 5D | Authority Gating | ✅ Complete | ✅ Yes |
| 5E | Proposal Envelope | ✅ Complete | ✅ Yes |
| 5F | Action Materialization | ✅ Complete | ✅ Yes |
| 5G | Dispatch Permission | ✅ Complete | ✅ Yes |
| 5H | Creation Readiness | ✅ Complete | ✅ Yes |
| 5I | Dispatch Persistence | 🔲 Future | — |
