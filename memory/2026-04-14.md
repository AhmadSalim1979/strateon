# Daily Notes — 2026-04-14

## WhatsApp Gateway Session Conflict (ongoing)

### Problem
WhatsApp gateway on +923215139934 entered a connect/disconnect loop starting ~07:50 Berlin time.
- Status codes: 499 (session conflict), 440 (session conflict that stops monitoring)
- Root cause: session drift when phone was offline for ~33min, compounded by conflicting linked
  device sessions on WhatsApp Web
- The gateway repeatedly restored creds from backup, reconnected, then got killed by WhatsApp again

### What fixed it (temporarily)
- `cd /root/OpenClaw && node openclaw.mjs channels login --channel "WhatsApp"` — re-links the session
- This worked temporarily but the conflict comes back within hours

### Permanent fix needed
On the phone (+923215139934):
1. Open WhatsApp → Settings → Linked Devices
2. Log out of ALL devices (wipe the session conflict)
3. Then re-run: `node openclaw.mjs channels login --channel "WhatsApp"`

### Key log lines
- "restored corrupted WhatsApp creds.json from backup" — happening repeatedly
- "status=499: session conflict" — WhatsApp killing the session
- "status=440: session conflict. Stopping web monitoring" — worse, stops entirely
- "✅ Linked! Credentials saved" — after re-link

### Files involved
- Creds: `/root/.openclaw/credentials/whatsapp/default/creds.json`
- Backup: `/root/.openclaw/credentials/whatsapp/default/creds.json.bak`
- Log: `/tmp/openclaw/openclaw-2026-04-14.log`

### Status as of 2026-04-15 00:00 UTC
- Re-linked at ~22:27 UTC (2026-04-14)
- Single 428 blip at 01:59 UTC, recovered immediately
- No flapping since re-link — mostly stable
