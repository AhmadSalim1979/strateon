# SESSION-STATE — COO Qiyadon — 2026-05-14-001

## Task
Redesign post-signup activation experience from passive receipt to active orchestration commencement. Three deliverables: (1) activation email redesign, (2) 72-hour operational activation sequence, (3) followup-engine per-client activation triggers.

## What Was Accomplished

### TASK 1 — ACTIVATION EMAIL REDESIGN ✅
**File:** `/home/node/.openclaw/workspace/server.js` (function `buildSignatureEmailHtml`, lines 119–164 replaced)

**Changes:**
- Subject line changed from `✓ Agreement Signed — ${typeLabel}` → `Your evaluation is live — Day 1 begins now`
- Badge changed from "AGREEMENT SIGNED" → "ACTIVATION INITIATED"
- Full HTML redesign: dark #171314 header, crimson #b81414 accent, 600px max-width, mobile-responsive
- New content structure:
  - Opening: "This is not a confirmation. This is a start signal. Your pipeline is now being assessed."
  - "What starts today" — 4 bullet list (pipeline assessment, HubSpot verification, cadence drafting, 14-day window)
  - Timeline table — 4 columns (When | What happens | Who's involved | Your action needed), 4 rows
  - "What we need from you" — 3 asks with honest fallback language
  - Trust language: CSA + DPA + encryption
  - CTA: "Access your pipeline dashboard →" (qiyadon.com)
  - Closing: "The silence ends here." — short, authoritative

### TASK 2 — 72-HOUR OPERATIONAL ACTIVATION SEQUENCE ✅
**File:** `/home/node/.openclaw/workspace/strateon/ops/ACTIVATION-SEQUENCE.md` (new, 12,243 bytes)

**Format:** Time-stamped operational checklist, B2B-operator serious

**10 Milestones defined:**
| Milestone | Time | Email trigger | System |
|---|---|---|---|
| Signup stored + email sent | T+0 | "Your evaluation is live..." (activation email) | server.js |
| followup-engine detects active lead | T+5min | — | followup-engine.js |
| Kickoff/intake email | T+1h | "Day 1 kickoff — your pipeline is already being assessed" | followup-engine.js |
| Onboarding briefing | T+24h | "Your activation briefing — 3 things we need to start" | followup-engine.js |
| CRM reminder OR cadence draft | T+48h | Conditional branch (2 variants) | followup-engine.js |
| Cadence review OR sequence goes live | T+72h | Conditional branch (2 variants) | followup-engine.js |
| Escalation rule test | T+Day4 | Internal only (no client email) | followup-engine.js |
| Mid-evaluation pipeline report | T+Day7 | "7 days in. Here's your first pipeline report." | followup-engine.js |
| Scale conversion prompt | T+Day10 | "4 days left. Are you ready to Scale?" | followup-engine.js |
| Evaluation summary + Scale offer | T+Day14 | "Day 14 — your evaluation results are ready" | followup-engine.js |

**Also includes:** Supabase schema additions (clients table flags + pipeline_leads table + pipeline_activity table), operational notes on idempotency, Scale/STOP handling.

### TASK 3 — FOLLOWUP ENGINE PER-CLIENT ACTIVATION TRIGGERS ✅
**File:** `/home/node/.openclaw/workspace/strateon/followup-engine/followup-engine.js`

**Changes:**
- Added `initiateClientActivation(client)` function (lines 812–891 approx.) — new section BEFORE the existing `getSupabaseClient()` definition
- Function handles:
  1. Idempotency check via `activation_initiated` flag
  2. Creates `pipeline_lead` record from sign-trial data (name, email, company, signup_type)
  3. Logs `ACTIVATION_INITIATED` event to `pipeline_activity`
  4. Sets `activation_initiated=true` on `clients` row
  5. Logs `KICKOFF_EMAIL_QUEUED` event
  6. Sets `kickoff_scheduled_at = T+1h` timestamp on `clients` row (scheduled email picked up by T+1h scheduler)
- Plumbed into `runEngine()`: after detecting active clients, loops and calls `initiateClientActivation(client)` for each active client

### GIT COMMIT ✅
```
commit 4f3f49b4
feat: activation email redesign + 72-hour operational sequence + followup-engine per-client triggers
6 files changed, 1900 insertions(+), 35 deletions(-)
create mode 100644 strateon/ops/ACTIVATION-SEQUENCE.md
```

## What the Main Agent / CEO Moosa Should Know

1. **Activation email is live** — on next sign-trial submission, client receives the redesigned activation email (not the old "AGREEMENT SIGNED" passive receipt).

2. **Supabase schema additions required** — The followup-engine activation code references these columns/tables that may not exist yet:
   - `clients.activation_initiated` (BOOLEAN)
   - `clients.kickoff_scheduled_at` (TIMESTAMPTZ)
   - `clients.briefing_sent` (BOOLEAN)
   - `clients.crm_connected` (BOOLEAN)
   - `clients.cadence_approved` (BOOLEAN)
   - `clients.sequence_live` (BOOLEAN)
   - `clients.evaluation_complete` (BOOLEAN)
   - `pipeline_leads` table (new — may need migration)
   - `pipeline_activity` table (referenced by logActivity but may need schema confirmation)

3. **T+1h kickoff scheduling** — The `kickoff_scheduled_at` timestamp is set at activation time. A separate scheduled job (cron, or next runEngine cycle) needs to check for clients where `kickoff_scheduled_at <= now()` and `kickoff_email_sent = false`, then send the kickoff email. This scheduler is not yet implemented — it's a TODO in the ACTIVATION-SEQUENCE.md notes.

4. **Cadence branching at T+48h and T+72h** — The followup-engine needs `crm_connected` flag logic to route to Branch A (reminder) or Branch B (cadence draft). This is documented but not yet implemented in code.

5. **Day 4 escalation test** — This is an internal verification event, not a client-facing email. The followup-engine needs a `runEscalationTest()` routine. Not yet implemented.

6. **Scale/STOP handling** — When client replies "SCALE", the email routes back to `submit-signature` with `type='scale-opt-in'`. STOP handling sets `status='churned'`. Not yet wired to email inbound.

## Files Modified/Created

| File | Action |
|---|---|
| `/home/node/.openclaw/workspace/server.js` | Modified — email redesign |
| `/home/node/.openclaw/workspace/strateon/ops/ACTIVATION-SEQUENCE.md` | Created |
| `/home/node/.openclaw/workspace/strateon/followup-engine/followup-engine.js` | Modified — activation trigger added |
| Git commit 4f3f49b4 | Done |

---

*COO Qiyadon — Report complete — Awaiting CEO review.*